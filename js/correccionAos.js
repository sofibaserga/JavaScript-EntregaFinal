/// Esto es una correccion que agregue para animaciones usadas en el curso de Desarrollo web, no esta relacionado al actual.

window.addEventListener('load', function () {
    AOS.init({
        once: true,
        delay: 200,
        duration: 800,
    });
});